{
  "properties": {
    "versionPaddls": {
      "type": "string",
      "x-prompt": "Which version of the library would you like to install ? (if empty, the latest version compatible will be installed)"
    }
  }
}